// ─────────────────────────────────────────────────────────────────────────────
// Placeholder OpenAPI types. Regenerate with:
//
//   API_URL=https://your-backend.up.railway.app pnpm api:types
//
// This emits the real schema into this file (overwrite is intentional).
// Until the backend has /v1/docs-json live, this stub lets the typed client
// compile by accepting any path/method shape.
// ─────────────────────────────────────────────────────────────────────────────

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type paths = Record<string, any>;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type components = Record<string, any>;
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type operations = Record<string, any>;

// Re-exports for convenience — once real types land, these are auto-emitted.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Schemas = Record<string, any>;
